
[project]

name = "utest"
description = "UTest is a simple unit-testing library."
requires-python = ">=3.14"

dependencies = []

keywords = [
  "unit",
  "test",
  "testing",
]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Typing :: Typed",
]
