# Tools for development

## Playwright

```sh
pip install playwright # Python wrapper of playwright.
playwright install # Installs browsers.
npm install -g @playwright/mcp@latest
claude mcp add --scope user playwright -- playwright-mcp
```
