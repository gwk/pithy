{
  'compile': ['$PROJ/legs/__main__.py test/modes.legs -test -output $NAME.swift'],
  'compile_timeout': 120,
  'cmd': './$NAME.swift',
  'args': [
    'a (bc def) g',
    '/* comment "contents" () / *. */',
    "'single quoted.' \"double quoted.\"",
    '/* nested /* comment. */ */',
    "'sq \(a) interpolated.'",
    '"dq \(a) interpolated."',
    "'sq \(nested \"interpolated \(a)\").'",
  ],
}